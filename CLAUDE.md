你在一个静态网页小工具仓库里工作：文本差异对比器。

硬要求：
1. 先保住引擎承重断言，再改页面。
2. 引擎绿不代表页面能用。任何页面改动都要过无头浏览器闸门。
3. 回写失败不许静默跳过。拿不到权限时，要明确写出“本次没有回写任何东西（不是回写成功）”并让 job 失败。
4. 共享回写工作流这里**有意**引用 `subinreum/ci-workflows/.github/workflows/report.yml@main`。这是可变引用，不是默认。每次 bump 或继续沿用前，先读上游 diff，再决定要不要跟。
